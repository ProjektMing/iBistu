<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.IBistu">

        <!--
          ============================================================
          Activity 注册规范
          ============================================================
          入口 Activity:
            android:name=".MainActivity"
            android:exported="true"
            (必需含 MAIN/LAUNCHER intent-filter)

          普通 Activity:
            android:name=".module.ClassNameActivity"
            android:exported="false"
            android:theme="@style/Theme.IBistu"

          Service 注册:
            android:name=".module.ClassNameService"
            android:exported="false"

          BroadcastReceiver 注册:
            android:name=".module.ClassNameReceiver"
            android:exported="false"

          ContentProvider 注册:
            android:name=".module.ClassNameProvider"
            android:authorities="edu.bistu.cs4029.ibistu.provider"
            android:exported="false"
          ============================================================
        -->

        <!-- 入口 Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.IBistu"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 自动静音 BroadcastReceiver -->
        <receiver
            android:name=".settings.AutoMuteReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="edu.bistu.cs4029.ibistu.ACTION_MUTE" />
                <action android:name="edu.bistu.cs4029.ibistu.ACTION_UNMUTE" />
                <action android:name="edu.bistu.cs4029.ibistu.ACTION_RESCHEDULE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".settings.AutoMuteBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name=".common.service.BaseForegroundService" android:foregroundServiceType="dataSync"/>

        <!-- 轮播内容 Provider -->
        <provider
            android:name=".text.SplashProvider"
            android:authorities="edu.bistu.cs4029.ibistu.text"
            android:exported="false" />

        <!-- 文件分享 Provider（导出 iCal） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="edu.bistu.cs4029.ibistu.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>